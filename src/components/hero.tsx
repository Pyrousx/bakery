import React from "react";

const pink = "#f1bfda";

const Hero = () => {
  return (
    <div className="bg-cover pt-10" style={{ backgroundColor: pink }}>
      hero
    </div>
  );
};

export default Hero;
